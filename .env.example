# Optional. Without this value the app uses its built-in assessment fixtures.
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sage_operations

# Set to disable only for a trusted local PostgreSQL instance.
